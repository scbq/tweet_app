<% content_for :title, "Nuevo Tweet" %>

<!-- Título centrado -->
<h1 class="display-4 text-center mb-4">Nuevo Tweet</h1>

<!-- Formulario de nuevo tweet -->
<%= render "form", tweet: @tweet %>

<!-- Enlace para volver al listado de tweets -->
<div class="text-center mt-4">
  <%= link_to tweets_path, class: 'btn btn-secondary d-inline-flex align-items-center' do %>
    <i class="fas fa-arrow-left me-2"></i> Volver a Tweets
  <% end %>
</div>
